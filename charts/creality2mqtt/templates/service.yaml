apiVersion: v1
kind: Service
metadata:
  name: {{ include "creality2mqtt.fullname" . }}
  labels:
    {{- include "creality2mqtt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "creality2mqtt.selectorLabels" . | nindent 4 }}
